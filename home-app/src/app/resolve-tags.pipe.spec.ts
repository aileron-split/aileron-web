import { ResolveTagsPipe } from './resolve-tags.pipe';

describe('ResolveTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
